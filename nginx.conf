user root;

events {}

http {
    include /etc/nginx/mime.types;

    server {
        listen 443 ssl;
        server_name location.ibetech.online;

        ssl_certificate /etc/letsencrypt/live/location.ibetech.online/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/location.ibetech.online/privkey.pem;

        location / {
            proxy_pass http://location-microservice:5010/;

            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }

        server {
            listen 80;
            server_name location.ibetech.online;

            return 301 https://$host$request_uri;
    }
}